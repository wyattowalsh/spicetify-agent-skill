# /spicetify Inert Assets

This directory contains local, inert templates and examples for installed-skill use.

These files are not the official Spicetify CLI, are not install scripts, and are not
trusted third-party code. They are examples that must still pass `/spicetify` audit,
dry-run planning, snapshot, confirmation, verification, and rollback gates before any
real user environment is changed.
