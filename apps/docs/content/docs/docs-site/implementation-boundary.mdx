---
title: Docs Site Implementation Boundary
description: What the docs app scaffold includes now and what remains approval-gated.
section: docs-site
source: curated
sourcePath: apps/docs/content/docs/archive/add-spicetify-skill/docs-site-implementation-plan.mdx
owner: docs
status: proposed
safetyLevel: read
---

# Docs Site Implementation Boundary

This docs app establishes the app root, content root, rendered docs routes,
content validation, and safety manifest without reading live Spotify or
Spicetify state.

## Present now

- `apps/docs/package.json` with local Next, Fumadocs, MDX, React, validation, and build scripts.
- Curated starter MDX pages with source-path frontmatter.
- `docs-site-manifest.json` for route, UI, validation, and safety policy.
- `content-map.json` for sidebar groups, mode coverage, schema references, and redaction rules.
- A local content validator that checks required pages, frontmatter, placeholders, and secret-like examples.
- Generated reference pages under `reference/` from schemas, OpenSpec specs, and mode docs.
- `tools/generate_docs_references.py --check` for deterministic generated-reference drift checks.
- Static `/docs` routes and `llms.txt` routes generated from repo-local docs metadata.

## Deferred until approval

- External registry component copying.
- Hosted or external search indexing.
- Local browser smoke tests beyond static build validation.
- Deployment, analytics, hosted search, feedback storage, DNS, or cloud credentials.
